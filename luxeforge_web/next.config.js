/** @type {import('next').NextConfig} */
const nextConfig = {
  // 'standalone' bundles a self-contained Node.js server, compatible with
  // both containerized deployments and Vercel (which handles it natively).
  output: 'standalone',
};

module.exports = nextConfig;
