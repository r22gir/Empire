# Empire Progress — Updated 2026-04-10

## EmpirePay Audit — Multi-Block Session

### Architecture Summary

**Two parallel crypto payment systems found:**

**System A — `crypto_payments.py` (canonical):**
- Uses SQLAlchemy models (`CryptoPayment`, `CryptoLedger`) with PostgreSQL
- Full payment lifecycle: create → confirm → ledger entries
- Supports BTC, ETH, SOL, USDC, USDT, BNB, BUSD, ADA, EMPIRE across bitcoin/ethereum/solana/bnb/cardano chains
- Discounts: 15% crypto, 20% EMPIRE token
- Wallet addresses derived deterministically via HMAC-SHA256 from `CRYPTO_MASTER_SEED`
- Has explicit failure: `_require_crypto()` raises HTTPException 503 if seed not set
- **NOT connected to finance DB** — mirrors payment record logic manually
- Endpoints registered at `/api/v1/crypto-payments` (tag: `crypto`)

**System B — `crypto_checkout.py` (legacy lightweight):**
- Uses SQLite finance DB directly
- Static wallet addresses from `.env` vars
- Only BTC/ETH/SOL/USDT with 10% discount (no EMPIRE)
- CoinGecko price fetching with 60-second cache
- Customer notifies → owner verifies manually
- **NOT using the SQLAlchemy models**
- Endpoints registered at `/api/v1/crypto-checkout` (tag: `crypto-checkout`)

**System C — `payments.py` (Stripe, separate concern):**
- SaaS subscriptions + invoice payment links via Stripe
- **Not EmpirePay** — separate Stripe-based payment system

### Schema Mismatch

`crypto_payments.py` uses `CryptoPaymentCreate` schema with `order_id`, `chain`, `token`, `expected_amount`. Response schema `CryptoPaymentResponse` includes `wallet_address` field. But `crypto_checkout.py` uses a completely different payload shape (`CryptoNotifyRequest` with `coin`, `tx_hash`, `sender_address`, `amount_crypto`, etc.).

### Missing Infrastructure (FIXED)

- `.env.example` has NO crypto configuration → **FIXED: Added crypto section**
- `crypto_payment_service.py` requires `CRYPTO_MASTER_SEED` env var → documented in `.env.example`
- `crypto_checkout.py` requires `CRYPTO_BTC_ADDRESS`, `CRYPTO_ETH_ADDRESS`, etc. → documented in `.env.example`
- **No DB migrations for `crypto_payments` / `crypto_ledger` tables** → **FIXED: Created alembic migration**
- **Pre-existing test bug** — test used "bitcoin" as invalid chain but it's now supported → **FIXED**

### Block Status

| Block | Status | Notes |
|-------|--------|-------|
| Block 1: EmpirePay source of truth | ✅ Complete | Canonical = `crypto_payments.py`, legacy = `crypto_checkout.py` |
| Block 2: All consumers | ✅ Complete | Frontend: EmpirePayPage, PaymentModule, CryptoCheckout. Backend: self-contained only |
| Block 3: Shared integration layer | ✅ Complete | Created alembic migration, updated .env.example, fixed test bug |
| Block 4: Update all consumers | ⬜ Pending | May not be needed — systems serve different purposes |
| Block 5: E2E verification | ⬜ Pending | Not started |

### Changes Made in Block 3

1. **Created alembic migration** `2026_04_10_0000-003_crypto_payments_add_crypto_tables.py`
   - Creates `crypto_payments` and `crypto_ledger` tables in PostgreSQL
   - Includes proper indexes on `order_id`, `status`, `payment_id`, etc.

2. **Updated `.env.example`** with crypto configuration section:
   - `CRYPTO_MASTER_SEED` for deterministic wallet derivation
   - `CRYPTO_PAYMENT_EXPIRY_MINUTES` for payment window
   - `CRYPTO_BTC_ADDRESS`, `CRYPTO_ETH_ADDRESS`, `CRYPTO_SOL_ADDRESS` for legacy checkout
   - `CRYPTO_USDT_TRC20_ADDRESS`, `CRYPTO_USDT_ERC20_ADDRESS`, `CRYPTO_USDC_ETH_ADDRESS`

3. **Fixed pre-existing test bug** in `test_crypto_payment_service.py`:
   - Changed "bitcoin" to "polygon" in `test_create_payment_invalid_chain_raises` since "bitcoin" is now a supported chain

### Frontend EmpirePay Consumers

| Component | API Called | Purpose |
|-----------|-----------|---------|
| `EmpirePayPage.tsx` | `/crypto-payments/chains` | Admin dashboard - view supported chains |
| `PaymentModule.tsx` | `/crypto-checkout/addresses` | Payment method picker - crypto option |
| `CryptoCheckout.tsx` | `/crypto-checkout/{invoice_id}`, `/crypto-checkout/{invoice_id}/notify` | Customer-facing crypto checkout |

### Key Files

- `backend/app/routers/crypto_payments.py` — canonical (521 lines)
- `backend/app/routers/crypto_checkout.py` — legacy (295 lines)
- `backend/app/services/crypto_payment_service.py` — service layer (240 lines)
- `backend/app/schemas/crypto_payment.py` — Pydantic schemas (71 lines)
- `backend/app/models/crypto_payment.py` — SQLAlchemy models (38 lines)
- `backend/alembic/versions/2026_04_10_0000-003_crypto_payments_add_crypto_tables.py` — **NEW** migration
- `empire-command-center/app/components/screens/EmpirePayPage.tsx` — admin UI (697 lines)
- `empire-command-center/app/components/business/payments/CryptoCheckout.tsx` — customer checkout
- `empire-command-center/app/components/business/payments/PaymentModule.tsx` — payment picker

---

## PRIOR SESSION (Apr 3-4)

## CORE SERVICES
- Backend :8000 — UP (888+ endpoints, 68+ routers)
- Command Center :3005 — UP
- OpenClaw :7878 — UP (32 skills)
- Ollama :11434 — UP

## SESSION HIGHLIGHTS (Apr 3-4)
- i18n + ConstructionForge + StoreFrontForge (137 new endpoints)
- RelistApp + LeadForge weaponized (55 new endpoints)
- Drawing Studio: 204 styles, 5 renderers, product catalog
- Unified job pipeline + invoices + payments
- AI desks executing real tasks
- Self-heal + maintenance engine
- LeadForge: 278 prospects, 3 campaigns, draft review + send
- 23 RelistApp research docs (500KB+)
- Feature audit + business system assessment
- LuxeForge: Send to Workroom + WoodCraft buttons

## COMPLETION: 96%
## PRODUCTS: 26 (24 active + 2 coming soon)
## ENDPOINTS: 888+
## DATABASE TABLES: 89 (+ 2 crypto tables via migration)
## TOTAL COMMITS: 660+
