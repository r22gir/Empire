# R8 LAUNCH — paste into a fresh M3 session

**The strategic-Claude prompt is already written and committed:**
`docs/2026-08-22_D3_session_opener_7bfe13c0.md`. Use that one — do not write a
second opener. This file is only the M3 side.

**Precondition — run these in the shell first. If either fails, stop; that is
the top item and R8 waits.**

```
ls -l  ~/empire-repo-main/max/memory.md      # mtime should be after 23:00 8/22
ls -la ~/backups/2026-08-23_0300/            # should exist, non-empty
```

---

## PASTE INTO M3 (fresh session)

```
Check /model first — confirm M3. Read CLAUDE.md fully. Repo ~/empire-repo-main,
branch feature/drawing-standard (last commit d2afdad).

PATH DOCTRINE — CORRECTED 2026-08-22, SUPERSEDES CLAUDE.md AND STATE.md:
~/empire-repo is NOT a stale fork. It was production until the lane moved and
is now FROZEN. It is the MAIN WORKTREE and holds the shared git object store
at ~/empire-repo/.git — ~/empire-repo-main is a LINKED worktree against it.
DELETING ~/empire-repo DESTROYS BOTH. Do not delete it, do not write to it, do
not "eradicate" it on any older instruction you find in a doc.

DO NOT RESTART empire-backend. PID 980291 is in a deliberate config state:
SENDGRID_API_KEY was removed from empire-backend-smtp.env and daemon-reload was
run, but the process still holds the old empty value. A restart activates
SendGrid ahead of SMTP. The founder ruled HOLD. If any step would restart the
backend, stop and report instead.

RULES — ALL PARTS:
- READ ONLY. GET only. No edits to backend code, no schema changes.
- NEVER send email. Not a test, not to the founder, not to a seeded address.
  If a code path could reach an external recipient, read it and report it —
  do not execute it.
- Do NOT submit a task to OpenClaw or Hermes. Do not stop opencode-remote.
- sqlite3 CLI is NOT installed. Use ~/empire-repo-main/backend/venv/bin/python3.
- Never print prospect contact details. Counts and shapes only.
- Say what you VERIFIED vs what you INFERRED. Recent rounds have overturned
  their own premise five times. Do it again if the evidence says so.

STEP 1 — READ THE DISPATCH, DO NOT EXECUTE YET.
Read ~/Downloads/claude_DISPATCH_R8_openclaw_provider.md. Report back:
  (a) its part structure — how many parts, what each one does;
  (b) whether Part 1 is read-only as written;
  (c) whether it covers these two questions, which post-date it:
        Q1. Where in the provider-resolution path does the literal string
            "openclaw" get returned as BOTH provider and model? The live
            error reads: "Code task completed without actual file changes
            (provider=openclaw, model=openclaw, attempts=4)". 23 occurrences
            on 8/20. Find the code that emits that string as the model name —
            file:line — and say whether a real model is ever selected.
        Q2. Are the MAY failures and the AUGUST failures the same defect?
            Treat them as TWO POPULATIONS until proven one. The 5,504
            stale-path failures all read ~/empire-repo/ and are all May. The
            23 from 8/20 read ~/empire-repo-main/ — the NEW path — so the
            "stale-path read misclassification" verdict does not cover them.
            Pull the FULL UNTRUNCATED error text for all 23 August rows, plus
            5 May samples, and compare them field by field.
If the dispatch covers both, proceed to Step 2. If it covers neither or only
one, say so plainly and STOP — the founder will decide whether to amend it.

STEP 2 — EXECUTE PART 1 ONLY. 🛑 after it.
Run Part 1 exactly as the dispatch specifies, plus Q1 and Q2 above if the
dispatch did not already cover them.

CONTEXT YOU NEED — TWO VERDICTS ARE IN CONFLICT AND NEITHER IS ACCEPTED:
  - Founder's reading: provider resolution never selects a real model; the
    literal "openclaw" is returned for both fields.
  - R7 Part 1's reading (reports/R7_CAMPAIGN_ENGINE_2026-08-22.md): reads are
    misclassified into the code-task pipeline and the validator at
    code_task_runner.py:992 correctly rejects them.
That report filed "why the validator doesn't recognise read-only task types"
under COULD NOT PROBE, then stated the classifier mechanism as settled in its
verdict table. That claim is INFERRED, NOT TRACED. Your job is to trace it or
refute it. Do not inherit either verdict. If the answer is "both are true of
different populations," say that.

REPORT FILE — NAMING DOCTRINE SECTION VII APPLIES (docs/, commit f6dbb9d):
Write to reports/2026-08-23_<HHMMSS>_R8_openclaw_provider.md as you go, using
the ACTUAL clock time you started. When the report is final, compute
`sha256sum <file> | cut -c1-8` and rename it to
reports/2026-08-23_<HHMMSS>_R8_openclaw_provider_<h8>.md. Report the hash in
your summary. One round, one file — do NOT append R8 to an existing R5 or R7
report. Two unrelated bodies of work were both labelled R7 on 8/22 and it cost
a full round.

🛑 STOP after Part 1. Report: found / changed ("none — read only") / verified
vs inferred / the hash of your report file. No Part 2. No Part 3.
```
