"""Estimate PDF helpers (reference / WIP — NOT Max's locked golden).

2026-09-25 founder correction: the empire-house-format pack + B2 flat-fold
sample are CURRENT Marleys work (content/numbers), close but NOT the golden
formatting Max must standardize on. Do not import these as Max's default
client-doc renderer Drawing/field golden is reference/max-golden/ (McLean). Estimates stay
on the portrait path — align Nelma/Empire branding only.

Kept here only so regen scripts can reproduce current Marleys EST-2026-272
numbers ($15,822.16) for reference.
"""

# Intentionally do NOT re-export house_format as the default estimate API.
# Callers that need the current Marleys WIP pack should import explicitly:
#   from app.services.estimates import house_format  # WIP reference only

__all__: list[str] = []
