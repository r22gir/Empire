# R6 NAV SWEEP — 2026-08-22
**Mode:** READ ONLY · **Repo:** `~/empire-repo-main` (branch `feature/drawing-standard`)
**Backend PID:** 967507 (untouched) · **Portal:** :3005 (untouched)

---

## PART 1 — BUILD THE THREE LISTS

### 1.1 — The nav (claimed) — 37 entries extracted from `LeftNav.tsx`

Source: `app/components/layout/LeftNav.tsx` lines 40–128.

| # | GROUP | ID | NAME | CLAIMED STATUS | KIND | SCREEN |
|---|---|---|---|---|---|---|
| 1 | Command | owner | Owner's Desk | active | product | — |
| 2 | Command | workroom | Empire Workroom | active | product | — |
| 3 | Command | craft | WoodCraft | active | product | — |
| 4 | Command | daily-summary | Daily Summary | active | daily-summary | — |
| 5 | Business | storefront | StoreFront Forge | active | product | — |
| 6 | Business | construction | ConstructionForge | active | product | — |
| 7 | Business | luxe | LuxeForge | active | product | — |
| 8 | Business | business-profile | Business Profile | active | screen | business-profile |
| 9 | Business | vendorops | VendorOps | active | product | — |
| 10 | Business | contractor | ContractorForge | active | product | — |
| 11 | Business | lead | LeadForge | active | product | — |
| 12 | Business | crm | ForgeCRM | active | product | — |
| 13 | Business | pay | EmpirePay | active | product | — |
| 14 | Business | pricing-studio | Pricing Studio | active | screen | pricing-studio |
| 15 | Tools | drawings | Drawing Studio | active | product | — |
| 16 | Tools | vision | AI Vision | active | product | — |
| 17 | Tools | recovery | RecoveryForge | active | product | — |
| 18 | Growth / Channels | social | SocialForge | active | product | — |
| 19 | Growth / Channels | market | MarketForge | active | product | — |
| 20 | Growth / Channels | support | SupportForge | active | product | — |
| 21 | Growth / Channels | ship | ShipForge | active | product | — |
| 22 | Growth / Channels | amp | AMP | active | product | — |
| 23 | Growth / Channels | archive | ArchiveForge | active | product | — |
| 24 | Growth / Channels | transcript | TranscriptForge | active | product | — |
| 25 | System | platform | PlatformForge | active | product | — |
| 26 | System | openclaw | OpenClaw | active | product | — |
| 27 | System | max-continuity | MAX Continuity | active | product | — |
| 28 | System | system | System | active | product | — |
| 29 | System | tokens | Tokens & Costs | active | product | — |
| 30 | System | hardware | Hardware | dev | product | — |
| 31 | More | relist | RelistApp | active | product | — |
| 32 | More | llc | LLCFactory | active | product | — |
| 33 | More | apost | ApostApp | active | product | — |
| 34 | More | assist | EmpireAssist | dev | product | — |
| 35 | More | vetforge | VetForge | planned | product | — |
| 36 | More | petforge | PetForge | planned | product | — |
| 37 | More | dev | Developer Panel | dev | product | — |

**Counts:**
- Total entries: **37**
- Claimed `active`: **32**
- Claimed `dev`: **3** (hardware, assist, dev)
- Claimed `planned`: **2** (vetforge, petforge)
- By kind: `product` = 34, `screen` = 2 (business-profile, pricing-studio), `daily-summary` = 1

---

### 1.2 — What pages actually exist + how product-id maps to component

**App subdirectories (real Next.js routes — independent of the nav switch):**
```
amp/            apostille/      archiveforge/   archiveforge-life/
channels/       intake/         landing/        luxe/
luxeforge/      max/            orchestration/  platform/
pricing/        transcriptforge-review/   woodcraft/   workroom/
```

**Pages found:** 18 `page.tsx` files at depth ≤ 2.

**Components directory (`app/components/`):**
- `screens/` — 51 files (one `<Module>Page.tsx` per module, plus generic screens like `ChatScreen`, `DashboardScreen`, `TasksScreen`, etc.)
- `business/` — 19 subdirs (catalog, costs, craftforge, crm, docs, drapery, finance, imports, inventory, jobs, payments, quotes, shared, shipping, support, templates, upholstery, vision)
- `tools/`, `layout/`, etc.

---

#### ⚠️ CRITICAL FINDING FOR PART 2: Product selection is **CLIENT-STATE ONLY**

There is **no `?product=` query parameter** in `app/page.tsx`. The component switch lives entirely in React `useState` inside `CommandCenter`:

```typescript
// app/page.tsx:115-117
const [activeProduct, setActiveProduct] = useState<EcosystemProduct>('owner');
const [activeScreen, setActiveScreen] = useState<ScreenMode>('chat');
```

**Only 2 of 37 entries can be URL-driven:**

| URL form | Result |
|---|---|
| `?screen=pricing-studio` or `#pricing-studio` | activeScreen = 'pricing-studio' → renders `<PricingStudioScreen />` |
| `#apostapp`, `#llc`, `#llcfactory` | activeProduct = 'apost' / 'llc' (PRODUCT_DEEP_LINKS map, page.tsx:109-113) |

**For the other 34 product-kind entries:** the URL `?product=<id>` does nothing — the page always lands on `activeProduct='owner' / activeScreen='chat'` unless state was previously set. A GET to `localhost:3005/?product=workroom` returns the **Owner's Desk**, not Workroom.

**Implication for Part 2:** the dispatch's curl-based screen-render probe (`localhost:3005/?product=<id>`) will produce **false ABSENT / false SHELL** results if naively applied. Part 2 must fall back to **static analysis of each component's fetch calls** to determine whether it actually wires to the backend, per the dispatch's explicit instruction: *"If product selection is client-side only and cannot be driven by URL, say so and fall back to evaluating the component's data calls statically. Do not fake a result."*

**Product-id → component switch (`renderCenterContent`, app/page.tsx:429-489):**

| nav id | Component rendered when activeScreen==='dashboard' | Source line |
|---|---|---|
| owner | `<DashboardScreen activeTab={...} />` | 434 |
| workroom | `<WorkroomPage initialSection={...} />` | 430 |
| craft | `<CraftForgePage initialSection={...} />` | 431 |
| social | `<SocialForgePage />` | 432 |
| platform | `<PlatformPage />` | 433 |
| luxe | `<LuxeForgePage onNavigate={...} />` | 435 |
| llc | `<LLCFactoryPage />` | 440 |
| assist | `<EmpireAssistPage />` | 441 |
| support | `<SupportForgePage />` | 442 |
| contractor | `<ContractorForgePage />` | 443 |
| lead | `<LeadForgePageNew initialSection={...} />` | 444 |
| market | `<MarketForgePage />` | 445 |
| pay | `<EmpirePayPage />` | 446 |
| ship | `<ShipForgePage />` | 447 |
| amp | `<AmpLanding />` (lazy) | 449 |
| crm | `<ForgeCRMPage />` | 451 |
| apost | `<ApostAppPage onNavigate={...} />` | 453 |
| recovery | `<RecoveryForgeScreen />` | 459 |
| relist | `<RelistAppPage initialSection={...} />` | 461 |
| archive | `<ArchiveForgePage />` | 463 |
| dev | `<DevPanel />` | 465 |
| openclaw | `<OpenClawTasksPage />` | 467 |
| max-continuity | `<MaxContinuityScreen />` | 469 |
| vendorops | `<VendorOpsPage />` | 471 |
| hardware | `<EcosystemProductPage ... />` (generic placeholder) | 473 |
| vetforge | `<VetForgePage />` | 475 |
| petforge | `<PetForgePage />` | 477 |
| construction | `<ConstructionForgePage initialSection={...} />` | 479 |
| storefront | `<StoreFrontForgePage initialSection={...} />` | 481 |
| transcript | `<TranscriptForgePage />` | 483 |
| vision | `<VisionAnalysisPage />` | 485 |
| drawings | `<DrawingStudioPage initialView={...} />` | 487 |

**Special handling (handleProductChange redirect, app/page.tsx:251-264):**
- `tokens` → sets activeScreen='costs' → renders `<CostTracker />` (lazy, app/page.tsx:502)
- `system` → sets activeScreen='report' → renders `<SystemReportScreen />` (app/page.tsx:498)
- `owner` → sets activeScreen='chat' → renders `<ChatScreen />` (app/page.tsx:505-524)
- `dev` → also reachable via activeScreen='dev' → renders `<DevPanel />` (app/page.tsx:504)

**Special handling (screens route directly, not via dashboard):**
- `business-profile` → activeScreen='business-profile' → `<BusinessProfileScreen />` (app/page.tsx:491)
- `pricing-studio` → activeScreen='pricing-studio' → `<PricingStudioScreen />` (app/page.tsx:493)

**Special non-routed:**
- `daily-summary` → toggles `showDashboard` boolean (LeftNav.tsx:166-170); does NOT change activeProduct/activeScreen. Renders `<RightPanel {...dashboardProps} />` inline (LeftNav.tsx:348-352).

**Result:** all 37 nav entries have a component path. No `ABSENT` entry from the 1.2 switch alone.

---

### 1.3 — Backend route prefix histogram (from `openapi.json`)

**Source:** `curl http://localhost:8000/openapi.json` (HTTP 200, PID 967507 confirmed up — uptime ~13 min).

**TOTAL ROUTES: 1086**

Full prefix histogram (count → prefix), 60 entries shown (matches the dispatch's `most_common(60)` call):

```
   79  max
   77  archiveforge
   55  leads
   37  relist
   32  construction
   32  storefront
   29  transcriptforge
   28  quotes
   25  vendorops
   24  finance
   24  recovery
   22  drawings
   21  socialforge
   20  chat-backup
   19  intake
   17  apostapp
   16  businessops
   16  jobs
   16  quotes-v2
   15  amp
   15  costs
   15  llcfactory
   14  craftforge
   14  patterns
   13  openclaw
   11  chats
   11  maintenance
   11  payments
   10  invoices
   10  vision
    9  crypto-payments
    9  inventory
    9  orchestration
    9  qr
    9  recovery-core
    8  fabrics
    8  files
    8  finance-legacy
    8  lifecycle
    8  memory
    7  economic
    7  kb
    7  notifications
    7  portal
    7  pricing
    6  crm
    6  dev
    6  inbox
    6  system
    6  tasks
    6  status
    5  ai
    5  custom-shapes
    5  customers
    5  photos
    5  tickets
    5  work-orders
    4  measurements
    4  avatar
    4  emails
```

(The dispatch's head_limit was implicit via `most_common(60)`; the list above is the full output of that call — no further prefixes were printed.)

**Note on prefix parsing:** the prefix is `p.split('/')[3]` (i.e. the 4th path segment). For `/api/v1/.../<prefix>/...`, this captures the top-level router prefix. Routes that don't fit that shape (e.g. `/openapi.json`, `/docs`, `/api/v1/health`) fall into a catch-all bucket (not visible at this top-60 cutoff).

---

### 1.3.1 — Mismatch sets (preliminary, before per-entry probing)

**A. Nav entries with no obvious backend prefix** (no matching `/api/v1/<name>/` route family in the top-60 histogram):

| nav id | name | closest backend prefix (if any) |
|---|---|---|
| craft | WoodCraft | `craftforge` (14 routes) — name-divergent but matches |
| max-continuity | MAX Continuity | **none** — no `continuity` prefix; nearest is `memory` (8) |
| tokens | Tokens & Costs | `costs` (15) |
| llc | LLCFactory | `llcfactory` (15) |
| apost | ApostApp | `apostapp` (17) |
| ship | ShipForge | **none** — no `ship` prefix; nearest is `shipping` (would be lazy-imported ShippingPage at activeScreen='shipping') |
| archive | ArchiveForge | `archiveforge` (77 — strong) |
| vetforge | VetForge | **none** — no `vet` prefix |
| petforge | PetForge | **none** — no `pet` prefix |
| daily-summary | Daily Summary | **N/A — panel, not a route** |
| hardware | Hardware | `system` (6 — `system/stats` is the endpoint EcosystemProductPage probes) |
| assist | EmpireAssist | **none** — no `assist` prefix |
| business-profile | Business Profile | `businessops` (16) — name-divergent but plausible |
| dev | Developer Panel | `dev` (6) |
| pricing-studio | Pricing Studio | `pricing` (7 — note: separate from public /pricing SaaS page) |

**B. Backend prefixes with no obvious nav entry:**

| backend prefix | routes | plausible nav id | gap reason |
|---|---|---|---|
| `leads` | 55 | lead (LeadForge) | name-divergent (`leads` vs `lead`) |
| `quotes-v2` | 16 | (covered by Workroom/Craft `quotes` section) | not a top-level nav product |
| `businessops` | 16 | business-profile | name-divergent |
| `chat-backup` | 20 | recovery (RecoveryForge) | name-divergent; `recovery` is separate |
| `maintenance` | 11 | **no match** | orphan backend prefix |
| `payments` | 11 | pay (EmpirePay) | name-divergent |
| `finance` / `finance-legacy` | 24+8 | **no match** | orphan prefixes (CraftForge `finance` section?) |
| `patterns` | 14 | **no match** | orphan (likely inside `drawings/`) |
| `orchestration` | 9 | **no match** | orphan |
| `qr` | 9 | **no match** | orphan |
| `recovery-core` | 9 | recovery (split with `recovery`) | split prefix |
| `fabric/` / `fabrics` | 8 | **no match** | orphan |
| `lifecycle` | 8 | **no match** | orphan |
| `memory` | 8 | max-continuity (plausible) | name-divergent |
| `notifications` | 7 | **no match** | orphan |
| `portal` | 7 | **no match** | orphan |
| `inbox` | 6 | **no match** | orphan (but reachable via ScreenMode 'inbox') |
| `status` | 6 | **no match** | orphan |
| `ai` | 5 | **no match** | orphan |
| `customers` | 5 | crm (ForgeCRM) | name-divergent |
| `photos` | 5 | vision (AI Vision) | name-divergent |
| `tickets` | 5 | support (SupportForge) | name-divergent |
| `work-orders` | 5 | **no match** | orphan |
| `measurements` | 4 | **no match** | orphan |
| `avatar` | 4 | **no match** | orphan |
| `emails` | 4 | **no match** | orphan |
| `crypto-payments` | 9 | pay (EmpirePay) | name-divergent |

**C. Nav entries with no matching `app/.../page.tsx` Next.js route AND no component:**

None — every nav id has a component (per 1.2 switch above).

---

🛑 **STOP per dispatch directive.** PART 1 complete. Awaiting founder go-ahead before PART 2.

---

## VERIFIED
- 37 nav entries extracted from `LeftNav.tsx:40-128` (lines 46, 47, 48, 50, 58, 59, 60, 63, 64, 65, 66, 67, 68, 74, 82, 83, 84, 92, 93, 94, 95, 96, 97, 98, 106, 107, 108, 109, 110, 111, 119, 120, 121, 122, 123, 124, 125).
- Backend at PID 967507 is up (uptime ~13 min at read time); `openapi.json` returns HTTP 200 with 1086 routes.
- Portal at `:3005` returns HTTP 200.
- Product selection is client-state only (`useState` in `app/page.tsx:115-117`). Only `pricing-studio` and `apost`/`llc`/`llcfactory` are URL-driven via `?screen=` / `#hash`.
- Product-id → component mapping extracted from `renderCenterContent` switch (`app/page.tsx:429-489`).

## INFERRED
- "Orphan" backend prefixes (e.g. `maintenance`, `orchestration`, `qr`, `lifecycle`, `finance-legacy`, `work-orders`, `measurements`, `avatar`, `emails`, `notifications`, `portal`, `status`, `ai`, `patterns`, `recovery-core`) MAY be reachable from existing screens but don't have a top-level nav entry — needs per-component fetch analysis in PART 2 to confirm.
- `recovery-core` and `recovery` both exist; the relationship between them is not visible from this probe alone.

## COULD NOT PROBE
- Whether the 18 `app/<route>/page.tsx` files are reachable via the Switch's deep-link handling for products other than `pricing-studio`, `apost`, `llc`. (Some Next.js routes like `/workroom`, `/platform`, `/luxeforge` exist as standalone pages, but `CommandCenter` (`app/page.tsx`) is the only route serving the nav switch. Standalone routes may be reachable but are NOT triggered by LeftNav clicks for most nav ids.)

---

## PART 2 — PROBE EACH OF THE 37 (static + live + data, NOT UI probes)

### 2.1 — DB prefix mapping used (declared explicitly)

Per dispatch rule 4: *"State the mapping you used — if a prefix is ambiguous, say so rather than assuming."*

**High-confidence prefixes (per dispatch or obvious):**
- `ag_` → ArchiveForge
- `sf2_` → StoreFront (sf2 = storefront v2)
- `cf_` → ContractorForge
- `mf_` → MarketForge *(dispatch hint — but: NO `mf_` tables found in this DB; closest match is `prospects` and `mf_products`? — see below)*
- `lf_` → LeadForge
- `vo_` → VendorOps
- `ra_` → RelistApp
- `sf_` → SupportForge (when no `sf2_` ambiguity)
- `apostille_*` → ApostApp
- `archive_*` (no underscore-prefixed tables) → ArchiveForge
- `intake_*` → LuxeForge (intake admin)
- `llc_*` → LLCFactory
- `craftforge_*` (no cf_ alias exists in DB) → AMBIGUOUS — could be ContractorForge OR CraftForge
- `quote_*`, `quotes_v2`, `quote_line_items`, `quote_photos` → Workroom (shared quotes)
- `invoice_*`, `payments_v2`, `payments` → shared (Workroom/Craft)
- `fabric`, `fabrics` → AMBIGUOUS — Workroom or shared catalog
- `customer_messages`/`client_*` → CRM or Workroom
- `expense*`, `finance_*` → shared
- `atlas_*` → Atlas desk (MAX sub)
- `max_*` → MAX
- `task*`, `tasks` → shared (MAX/Workroom/Craft)
- `recovery_*` → RecoveryForge
- `social_*` → SocialForge
- `token_usage` → Token tracking (costs desk)
- `catalog_*` → ProductCatalog
- `design_sessions` → AMBIGUOUS — DrawingStudio or Vision

**AMBIGUOUS tables (no clear prefix):**
- `customers` (171 rows) — could be CRM (`customers`) or StoreFront (`sf2_customers`)
- `contacts` (7 rows) — could be ContractorForge (`contacts/`) or shared CRM
- `vendors` (51 rows) — could be VendorOps or Workroom vendor list
- `shipments` (0 rows) — could be ShipForge or shared
- `label_catalog` (0 rows) — could be ShipForge or shared
- `prospects` (322 rows) — could be LeadForge (`lf_prospects: 0` so NOT this!) or shared outbound
- `mf_products` (11 rows) — dispatch says `mf_` = MarketForge, so this maps cleanly

The dispatch did not cover every prefix in this DB. Where I'm uncertain I marked **AMBIGUOUS** in the verdict table.

### 2.2 — Static fetch-call inventory per nav entry

Per the dispatch's grep pattern (`fetch(` / `/api/v1`), with API-base resolution from `app/lib/api.ts` (defaults to `http://localhost:8000/api/v1` when localhost) and component-local constants:

| nav id | component | backend calls discovered |
|---|---|---|
| owner | DashboardScreen | `GET /max/accuracy/stats?days=7` |
| workroom | WorkroomPage | `GET /quotes-v2`, `GET /crm/customers`, `GET /jobs/`, `GET /finance/dashboard`, `GET /inventory/dashboard`, `GET /tasks/`, `GET /max/ai-desks/tasks`, `DELETE /quotes-v2/{id}`, `PUT/POST /tasks/{id}` |
| craft | CraftForgePage | `GET /craftforge/dashboard`, `GET /craftforge/jobs?limit=5`, `GET /craftforge/designs?limit=5` |
| social | SocialForgePage | `GET/POST/DELETE/PUT ${SF_API}/dashboard\|posts\|accounts\|sync\|ai-guide` |
| platform | PlatformPage | reads `/system/stats`, `/system/metrics`, `/businessops/health` (live from API), exposes live state |
| luxe | LuxeForgePage | `GET/POST/DELETE /intake/admin/projects\|users\|archived\|to-quote\|restore`, image paths under API_BASE |
| llc | LLCFactoryPage | `GET/POST /llcfactory/dashboard\|services\|packages\|orders\|customers\|documents\|name-check\|generate-oa\|generate-docs` |
| assist | EmpireAssistPage | `GET /costs/transactions?limit=20` |
| support | SupportForgePage | `GET /tickets/`, `GET /kb/articles`, `GET /kb/categories`, `GET /crm/customers` |
| contractor | ContractorForgePage | `GET /jobs/`, `GET /contacts/`, `GET /finance/invoices` |
| lead | LeadForgePageNew | `GET/POST ${LF_API}/leadforge/prospects\|prospect-pipeline\|campaigns\|drafts\|send\|enroll\|execute\|followups\|activity\|search` |
| market | MarketForgePage | `GET ${_API_BASE}/listings`, `GET ${_API_BASE}/preorders/` (routes NOT under `/api/v1/`) |
| pay | EmpirePayPage | `GET /crypto-payments/?limit=N`, `GET /crypto-payments/chains`, `GET /finance/invoices` |
| ship | ShipForgePage | `${SHIPPING_API} = ${API_BASE}/shipping` (NOT `/api/v1/shipping`!) → `GET /shipping/history`, `POST /shipping/rates\|labels`, `GET /shipping/track/{num}` |
| amp | `amp/page.tsx` (489 lines) | **NONE — static SPA shell, hardcoded HUBSPOT_BASE images, no fetch calls** |
| crm | ForgeCRMPage | `GET /crm/pipeline`, `GET /crm/customers?limit=500`, `POST /crm/customers` |
| apost | ApostAppPage | `GET/POST/PUT/DELETE /apostapp/orders\|forms/generate`, `GET /llcfactory/apostille` |
| recovery | RecoveryForgeScreen | `GET/POST /recovery/images\|status\|categories\|reanalysis-queue/*\|{key}/tags\|{key}/scrap\|{key}/reanalyze\|identify` |
| relist | RelistAppPage | `GET/POST /relist/analytics/dashboard\|sources\|scout/run\|listings/from-source\|sources/{id}/analyze\|deals\|listings/{id}/cross-list\|orders\|services\|bulk-refresh\|by-platform` |
| archive | ArchiveForgePage | `GET/POST /archiveforge/reference/search\|life-issues/search\|uploads/{id}\|identify\|google-books/lookup\|{id}/issue-info\|{id}/photos\|{id}/confirm-reference\|{id}/resolve-issue-info` |
| dev | DevPanel | `GET /dev/status`, `GET /dev/git`, `GET /dev/audit?limit=20`, `GET /dev/health` |
| openclaw | OpenClawTasksPage | `GET/POST/PUT/DELETE /openclaw/tasks\|stats` |
| max-continuity | MaxContinuityScreen → `ContinuityPanel` | `GET /max/status`, `GET /max/evaluation/scores?limit=5`, `GET /max/memory-status`, `GET /openclaw/health`, `POST /max/chat` |
| vendorops | VendorOpsPage | `${API}${path}` where path is dynamic (e.g. `/vendorops/plans`, `/vendorops/status`) |
| hardware | EcosystemProductPage | dynamic via `PRODUCT_MAP[productId].endpoints`; for `hardware` that resolves to `/system/stats` |
| vetforge | VetForgePage (83 lines) | **NONE — pure placeholder page, hardcoded "Coming Soon" copy** |
| petforge | PetForgePage (83 lines) | **NONE — pure placeholder page, hardcoded "Coming Soon" copy** |
| construction | ConstructionForgePage | `${CF_API} = ${API}/construction` → `GET /construction/projects\|projects/{id}/dashboard\|projects/1/lots\|buyers\|projects/1/sales/pipeline\|projects/1/payments/overdue\|contractors\|projects/1/materials` |
| storefront | StoreFrontForgePage | `${SF_API} = ${API}/storefront` → `GET /storefront/products\|transactions\|customers\|inventory/1\|employees` |
| transcript | TranscriptForgePage | `GET/POST /transcriptforge/jobs\|jobs/{id}/upload\|jobs/{id}/chunks/{cid}/review\|jobs/{id}/continue\|jobs/{id}/approve\|jobs/{id}/transcript/{kind}\|jobs/{id}/stop` |
| vision | VisionAnalysisPage | `GET /crm/customers` ONLY (despite `/vision/*` having 10 backend routes — **the component does not call its own backend**) |
| drawings | DrawingStudioPage | `POST /drawings/analyze-furniture`, `POST /drawings/analyze-sketch`, `POST /drawings/bench`, `POST /drawings/general`, `POST /drawings/generate`, `POST /max/chat` |
| business-profile | BusinessProfileScreen | `GET/POST PROFILE_API = ${API}/socialforge/profile` (**uses `/socialforge/` prefix, not `/businessops/`**) |
| pricing-studio | PricingStudioScreen | `GET /pricing/canonical/status`, `GET /pricing/labor-rates`, `POST /pricing/workroom/calculate`, `POST /pricing/woodcraft/calculate` |
| tokens | CostTracker (lazy) | dynamic `fetch(url)` from a list, generally `/costs/summary\|today\|by-model\|daily` |
| system | SystemReportScreen | `GET /max/system-report`, `GET /max/changelog` |
| daily-summary | RightPanel (panel, not page) | `GET /max/telegram/status`, `GET /vision/history?limit=8`, `GET /tasks/?limit=4`, `GET /max/quality/metrics`, `GET /max/intelligence/brief`, `GET /max/intelligence/weekly`, `GET /max/security/stats`, `GET /max/intelligence/cost-per-desk`, `POST /max/intelligence/brief/generate` |

### 2.3 — Live route probes (one representative GET per nav entry)

**Verbatim command/output snippets for all 37 (most representative GET):**

```
owner              /api/v1/max/accuracy/stats             | HTTP=200 | 944   b | HAS-DATA
workroom           /api/v1/quotes-v2                      | HTTP=200 | 193587b | HAS-DATA  (49 quotes_v2 in DB)
craft              /api/v1/craftforge/quotes             | HTTP=200 | 1502  b | HAS-DATA
social             /api/v1/socialforge/posts              | HTTP=200 | 6111  b | HAS-DATA  (12 social_accounts)
platform           /api/v1/system/stats                   | HTTP=200 | 1508  b | HAS-DATA
luxe               /api/v1/intake/admin/projects          | HTTP=401 | 30    b | AUTH  (503 intake_projects in DB)
llc                /api/v1/llcfactory/services            | HTTP=200 | 6596  b | HAS-DATA
assist             /api/v1/costs/transactions             | HTTP=200 | 30748 b | HAS-DATA
support            /api/v1/tickets/                       | HTTP=200 | 57    b | EMPTY  `{"tickets":[],"total":0,...}`
contractor         /api/v1/jobs/active                    | HTTP=200 | 10108 b | HAS-DATA
lead               /api/v1/leads/                         | HTTP=200 | 22    b | EMPTY  `{"leads":[],"total":0}`
market             /listings (no /api/v1)                | HTTP=401 | 30    b | AUTH
pay                /api/v1/crypto-payments/               | HTTP=200 | 2     b | EMPTY  `[]`
ship               /shipping/history  (no /api/v1)       | HTTP=200 | 2     b | EMPTY  `[]`
amp                /api/v1/amp/me                         | HTTP=401 | 30    b | AUTH  (component has NO fetches; amp/page is static)
crm                /api/v1/crm/pipeline                   | HTTP=200 | 20053 b | HAS-DATA  (143 draft quotes, 171 customers in DB)
apost              /api/v1/apostapp/                      | HTTP=200 | 1818  b | HAS-DATA
recovery           /api/v1/recovery/images                | HTTP=200 | 457456b | HAS-DATA  (massive)
relist             /api/v1/relist/sources                 | HTTP=200 | 14072 b | HAS-DATA  (18 ra_listings)
archive            /api/v1/archiveforge/reference/search  | HTTP=200 | 358   b | HAS-DATA  (1863 archive_life_issue_master)
dev                /api/v1/dev/status                     | HTTP=200 | 365   b | HAS-DATA
openclaw           /api/v1/openclaw/tasks                 | HTTP=200 | 69373 b | HAS-DATA  (7390 openclaw_tasks!)
max-continuity     /api/v1/memory/list (ContinuityPanel)  | HTTP=405 | 31    b | POST-only — endpoint exists
vendorops          /api/v1/vendorops/plans                | HTTP=200 | 774   b | HAS-DATA  (51 vendors)
hardware           /api/v1/system/stats                   | HTTP=200 | 1508  b | HAS-DATA  (via generic PRODUCT_MAP)
vetforge           /api/v1/vetforge/ping                  | HTTP=404 | 22    b | NO-BACKEND
petforge           /api/v1/petforge/ping                  | HTTP=404 | 22    b | NO-BACKEND
construction       /api/v1/construction/projects          | HTTP=200 | 1049  b | HAS-DATA  (3 cf_projects, 60 cf_lots)
storefront         /api/v1/storefront/products            | HTTP=200 | 20988 b | HAS-DATA  (50 sf2_products)
transcript         /api/v1/transcriptforge/jobs           | HTTP=200 | 2889  b | HAS-DATA
vision             /api/v1/crm/customers (per component)  | HTTP=200 | 59    b | HAS-DATA (wrong endpoint) — /api/v1/vision/status also works at 567b but COMPONENT DOESN'T CALL IT
drawings           /api/v1/drawings/bench                 | HTTP=405 | 31    b | POST-only — endpoint exists
business-profile   /api/v1/socialforge/profile            | HTTP=200 | 1479  b | HAS-DATA  (uses /socialforge prefix not /businessops)
pricing-studio     /api/v1/pricing/canonical/status       | HTTP=200 | 346   b | HAS-DATA
tokens             /api/v1/costs/summary                  | HTTP=200 | 2326  b | HAS-DATA
system             /api/v1/max/system-report              | HTTP=200 | 5544  b | HAS-DATA
daily-summary      /api/v1/max/intelligence/brief         | HTTP=200 | 1363  b | HAS-DATA
```

### 2.4 — DB row counts (relevant subset)

```
openclaw_tasks                = 7390
quotes_v2                     = 49
customers                     = 171
intake_projects               = 503
cf_projects                   = 3
cf_lots                       = 60
cf_materials                  = 10
cf_contractors                = 3
ag_archives                   = 73
ag_archive_photos             = 59
archive_life_issue_master     = 1863
prospects                     = 322  (AMBIGUOUS: LeadForge router empty, this table has data)
lf_leads                      = 0
lf_prospects                  = 0
sf2_products                  = 50
sf2_inventory                 = 50
sf2_customers                 = 10
fabrics                       = 12    (no nav entry uses this router)
work_orders                   = 0
work_order_items              = 0
crypto_transactions           = 0
crypto_wallets                = 0
invoices                      = 32
vendors                       = 51
shipments                     = 0
social_accounts               = 12
listings                      = 2
mf_products                   = 11
contacts                      = 7
tasks                         = 1861
intake_users                  = 654
```

---

## VERDICT TABLE — All 37 nav entries

`OBSERVED` vocabulary per dispatch: LIVE / WIRED-EMPTY / SHELL / ABSENT / UNKNOWN.

| # | GROUP | ID | NAME | CLAIMED | COMPONENT | ROUTES CALLED | DATA ROWS | OBSERVED | EVIDENCE |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Command | owner | Owner's Desk | active | DashboardScreen | /max/accuracy/stats | (no DB table) | **LIVE** | 200, 944b payload |
| 2 | Command | workroom | Empire Workroom | active | WorkroomPage | /quotes-v2, /crm/customers, /jobs/, /finance/dashboard, /inventory/dashboard, /tasks/ | quotes_v2=49, customers=171, jobs=8, intake_projects=503 | **LIVE** | 200, 193KB on /quotes-v2 |
| 3 | Command | craft | WoodCraft | active | CraftForgePage | /craftforge/dashboard\|jobs\|designs\|quotes | (cf_ tables) | **LIVE** | 200, 1502b on /craftforge/quotes |
| 4 | Command | daily-summary | Daily Summary | active | RightPanel (panel toggle) | /max/intelligence/brief\|weekly\|cost-per-desk\|security/stats\|telegram/status\|quality/metrics, /vision/history, /tasks/ | (no per-table mapping) | **LIVE** | 200, 1363b on /max/intelligence/brief |
| 5 | Business | storefront | StoreFront Forge | active | StoreFrontForgePage | /storefront/products\|transactions\|customers\|inventory/1\|employees | sf2_products=50, sf2_inventory=50, sf2_customers=10 | **LIVE** | 200, 20KB on /storefront/products |
| 6 | Business | construction | ConstructionForge | active | ConstructionForgePage | /construction/projects\|lots\|buyers\|sales/pipeline\|payments/overdue\|contractors\|materials | cf_projects=3, cf_lots=60, cf_materials=10, cf_contractors=3 | **LIVE** | 200, 1049b on /construction/projects |
| 7 | Business | luxe | LuxeForge | active | LuxeForgePage | /intake/admin/projects\|users\|archived\|to-quote\|restore | intake_projects=503, intake_users=654 | **UNKNOWN** | 401 (auth required); cannot probe without token — DB strongly suggests real data behind auth |
| 8 | Business | business-profile | Business Profile | active | BusinessProfileScreen | /socialforge/profile (NOT /businessops) | business_profiles=2 | **LIVE** | 200, 1479b (uses /socialforge prefix, not the obvious /businessops) |
| 9 | Business | vendorops | VendorOps | active | VendorOpsPage | /vendorops/plans\|status\|activation (dynamic) | vendors=51, vo_subscriptions=1, vo_renewal_alerts=1 | **LIVE** | 200, 774b on /vendorops/plans |
| 10 | Business | contractor | ContractorForge | active | ContractorForgePage | /jobs/\|contacts/\|finance/invoices | jobs=8, contacts=7, invoices=32 | **LIVE** | 200, 10KB on /jobs/active |
| 11 | Business | lead | LeadForge | active | LeadForgePageNew | /leads\|leadforge/prospects\|... | lf_leads=**0**, lf_prospects=**0** (but `prospects`=322 in AMBIGUOUS table) | **WIRED-EMPTY** | 200, `{"leads":[],"total":0}`; LeadForge router returns empty; the 322 prospects live elsewhere |
| 12 | Business | crm | ForgeCRM | active | ForgeCRMPage | /crm/pipeline\|customers | customers=171, pipeline shows 143 draft quotes | **LIVE** | 200, 20KB on /crm/pipeline |
| 13 | Business | pay | EmpirePay | active | EmpirePayPage | /crypto-payments/\|chains, /finance/invoices | crypto_transactions=**0**, crypto_wallets=**0**, invoices=32 | **WIRED-EMPTY** | crypto route returns `[]`; invoice route has data — partial wire |
| 14 | Business | pricing-studio | Pricing Studio | active | PricingStudioScreen | /pricing/canonical/status\|labor-rates\|workroom/calculate\|woodcraft/calculate | (pricing tables not enumerated) | **LIVE** | 200, 346b on /pricing/canonical/status |
| 15 | Tools | drawings | Drawing Studio | active | DrawingStudioPage | POST /drawings/bench\|general\|generate\|analyze-furniture\|analyze-sketch, /max/chat | (drawing tables) | **LIVE** | 405 on GET means POST endpoints exist; dispatch notes Roman shades live |
| 16 | Tools | vision | AI Vision | active | VisionAnalysisPage | **/crm/customers ONLY** (does NOT call /vision/*) | /api/v1/vision/* has 10 routes; vision/status works (567b) | **SHELL** | component is wired to wrong router; its own vision backend exists but component doesn't reach it |
| 17 | Tools | recovery | RecoveryForge | active | RecoveryForgeScreen | /recovery/images\|status\|categories\|reanalysis-queue/* | openclaw_tasks=7390 (Layer 3 also lives here) | **LIVE** | 200, 457KB on /recovery/images (huge) |
| 18 | Growth | social | SocialForge | active | SocialForgePage | /socialforge/dashboard\|posts\|accounts | social_accounts=12, social_post_results=0 | **LIVE** | 200, 6KB on /socialforge/posts |
| 19 | Growth | market | MarketForge | active | MarketForgePage | /listings\|preorders/ (NO /api/v1/) | listings=2, mf_products=11 | **UNKNOWN** | 401 (auth required); cannot probe without token |
| 20 | Growth | support | SupportForge | active | SupportForgePage | /tickets/\|kb/articles\|kb/categories\|crm/customers | sf_tickets=3 (DB), but /api/v1/tickets/ returns `{"tickets":[],"total":0}` | **WIRED-EMPTY** | 200, 57b empty envelope despite sf_tickets=3 — DB/API mismatch |
| 21 | Growth | ship | ShipForge | active | ShipForgePage | /shipping/history\|labels\|rates\|track (no /api/v1/) | shipments=**0**, label_catalog=**0** | **WIRED-EMPTY** | 200, `[]` on /shipping/history; zero rows |
| 22 | Growth | amp | AMP | active | `amp/page.tsx` (489-line static SPA) | **NONE** | (no per-table mapping; amp tables absent) | **SHELL** | page has zero fetch calls; static hardcoded HUBSPOT_BASE content |
| 23 | Growth | archive | ArchiveForge | active | ArchiveForgePage | /archiveforge/reference/search\|life-issues/search\|uploads\|identify\|google-books\|issue-info\|photos\|confirm-reference | ag_archives=73, ag_archive_photos=59, archive_life_issue_master=1863, archive_life_issue_sources=1871 | **LIVE** | 200, 358b on /archiveforge/reference/search |
| 24 | Growth | transcript | TranscriptForge | active | TranscriptForgePage | /transcriptforge/jobs\|upload\|chunks/{cid}/review\|continue\|approve\|transcript/{kind}\|stop | (transcript tables) | **LIVE** | 200, 2889b on /transcriptforge/jobs |
| 25 | System | platform | PlatformForge | active | PlatformPage | /system/stats\|metrics, /businessops/health, /max/memory | system endpoints live | **LIVE** | 200, 1508b on /system/stats |
| 26 | System | openclaw | OpenClaw | active | OpenClawTasksPage | /openclaw/tasks\|stats | openclaw_tasks=**7390** | **LIVE** | 200, 69KB on /openclaw/tasks — but the dispatch states OpenClaw has ~80% failure rate at 27 tasks/57d; "active" is misleading |
| 27 | System | max-continuity | MAX Continuity | active | MaxContinuityScreen → ContinuityPanel | /max/status\|evaluation/scores\|memory-status, /openclaw/health, /max/chat | (MAX tables) | **LIVE** | ContinuityPanel calls real endpoints (per static grep) |
| 28 | System | system | System | active | SystemReportScreen | /max/system-report\|changelog | (no per-table mapping) | **LIVE** | 200, 5544b on /max/system-report |
| 29 | System | tokens | Tokens & Costs | active | CostTracker (lazy) | /costs/summary\|today\|by-model\|daily | token_usage=0 (token table empty) | **LIVE** | 200, 2326b on /costs/summary — costs endpoint works even though token_usage table is empty |
| 30 | System | hardware | Hardware | dev | EcosystemProductPage (generic placeholder) | /system/stats (via PRODUCT_MAP) | system endpoints live | **LIVE** | 200, 1508b — claims "dev" but the generic page reads /system/stats; closer to LIVE than DEV |
| 31 | More | relist | RelistApp | active | RelistAppPage | /relist/analytics/dashboard\|sources\|scout/run\|listings\|orders\|deals\|services\|bulk-refresh\|by-platform | ra_listings=18, ra_services=16, ra_source_products=13 | **LIVE** | 200, 14KB on /relist/sources |
| 32 | More | llc | LLCFactory | active | LLCFactoryPage | /llcfactory/dashboard\|services\|packages\|orders\|customers\|documents\|name-check\|generate-oa\|generate-docs | llc_formations=**0** | **WIRED-EMPTY** | 200, 6596b on /llcfactory/services (services data), but llc_formations=0 — primary purpose unfulfilled |
| 33 | More | apost | ApostApp | active | ApostAppPage | /apostapp/orders\|forms/generate, /llcfactory/apostille | apostille_documents=0 | **LIVE** | 200, 1818b on /apostapp/ — orders endpoint returns data structure even with 0 apostille docs |
| 34 | More | assist | EmpireAssist | dev | EmpireAssistPage | /costs/transactions | (costs tables) | **LIVE** | 200, 30KB on /costs/transactions — claims "dev" but renders real costs data |
| 35 | More | vetforge | VetForge | planned | VetForgePage (83-line placeholder) | NONE | (no vetforge tables in DB) | **SHELL** | empty static placeholder; no fetches; no backend |
| 36 | More | petforge | PetForge | planned | PetForgePage (83-line placeholder) | NONE | (no petforge tables in DB) | **SHELL** | empty static placeholder; no fetches; no backend |
| 37 | More | dev | Developer Panel | dev | DevPanel | /dev/status\|git\|audit\|health | dev tables | **LIVE** | 200, 365b on /dev/status — claims "dev" but functional |

### Tally

| OBSERVED | count | nav ids |
|---|---|---|
| **LIVE** | 26 | owner, workroom, craft, daily-summary, storefront, construction, business-profile, vendorops, contractor, crm, pricing-studio, drawings, recovery, social, archive, transcript, platform, openclaw, max-continuity, system, tokens, hardware, relist, apost, assist, dev |
| **WIRED-EMPTY** | 5 | lead, pay, support, ship, llc |
| **SHELL** | 4 | vision, amp, vetforge, petforge |
| **UNKNOWN** (auth barrier) | 2 | luxe, market |
| **ABSENT** | 0 | — |

### Claimed ≠ Observed mismatches (the headline number)

**9 strict mismatches** (claimed `active` but observed something less than LIVE):

| nav id | claimed | observed | reason |
|---|---|---|---|
| **vision** | active | SHELL | component fetches /crm/customers only; its own /vision/* backend (10 routes, status works) is unreachable from the component |
| **amp** | active | SHELL | amp/page.tsx is a 489-line static SPA shell with zero fetch calls; hardcoded HUBSPOT_BASE images |
| **support** | active | WIRED-EMPTY | /api/v1/tickets/ returns `{"tickets":[],"total":0}` despite `sf_tickets` table having 3 rows — DB/API mismatch |
| **lead** | active | WIRED-EMPTY | /api/v1/leads/ returns `{"leads":[],"total":0}`; `lf_leads` and `lf_prospects` are both 0 (322 prospects live in an AMBIGUOUS `prospects` table the router may not query) |
| **pay** | active | WIRED-EMPTY | /api/v1/crypto-payments/ returns `[]`; `crypto_transactions` and `crypto_wallets` are both 0 (the /finance/invoices portion of the page DOES return data — partial wire) |
| **ship** | active | WIRED-EMPTY | /shipping/history returns `[]`; `shipments`=0 and `label_catalog`=0 |
| **llc** | active | WIRED-EMPTY | `llc_formations`=0 (the primary purpose); only services/packages data exists |
| **luxe** | active | UNKNOWN | 401 auth required; cannot probe — but `intake_projects`=503 suggests real data behind auth |
| **market** | active | UNKNOWN | 401 auth required; cannot probe — `listings`=2, `mf_products`=11 in DB |

If counting UNKNOWN as a mismatch (active should not be UNKNOWN), **11 mismatches**.

**3 under-claim mismatches** (claimed `dev` but observed LIVE — overcautious):

| nav id | claimed | observed | reason |
|---|---|---|---|
| hardware | dev | LIVE | generic PRODUCT_MAP page reads /system/stats (1508b) |
| assist | dev | LIVE | /costs/transactions returns 30KB |
| dev | dev | LIVE | /dev/status returns 365b |

These are NOT bugs — they are over-conservative claims that mask working capability from the founder. They are noted because the dispatch asks for the gap, not just the negative.

**vetforge and petforge are consistently "planned + SHELL"** — the claimed status matches reality (planned = empty shell, which is what they are). They are NOT counted as mismatches.

---

## ORPHAN ROUTE PROBES

Per dispatch: GET one representative route per orphan prefix. 16 prefixes inspected.

| prefix | best-route probe | HTTP | bytes | has data? | reachable from UI? |
|---|---|---|---|---|---|
| maintenance | /api/v1/maintenance/status | 200 | 205 | **YES** (config: 17 pending, 1 reverted) | NO — no nav entry, no component import |
| orchestration | /api/v1/max/orchestration/status | 200 | 30948 | **YES** (huge role/entrypoints config) | NO |
| qr | /api/v1/qr/cookie-policy | 200 | 708 | YES (cookie policy text) | NO (only one documented public endpoint) |
| fabrics | /api/v1/fabrics?limit=10 | 200 | 8030 | **YES** — 12 fabric rows match DB | NO — `app/components/business/drapery/` exists but no LeftNav entry calls /fabrics |
| lifecycle | /api/v1/archiveforge/{archive_id}/lifecycle | (nested) | — | YES — `archive_item_lifecycle`=4, `archive_item_lifecycle_events`=9 | YES (via archiveforge page) |
| notifications | /api/v1/notifications | 200 | 288 | **YES** — 1 notification "194 inbox items" | NO (no nav entry uses it; only inline in RightPanel) |
| portal | /api/v1/payments/portal | 422 | 107 | endpoint exists, needs params | NO |
| status | nested in /api/v1/apostapp/orders/{order_id}/documents/{doc_index}/status | — | — | (subroute of apostapp) | YES (via apostapp) |
| ai | /ai/ai/enhance-description | 405 | 31 | endpoint exists, POST only | NO |
| **work-orders** | /api/v1/work-orders?limit=10 | 200 | 28 | `{"work_orders":[],"total":0}` — **EMPTY** | NO — `work_orders` table has 0 rows anyway; capability appears unused |
| **measurements** | /api/luxeforge/measurements/calculate | GET=404, POST=422 | 51 | **ENDPOINTS REGISTERED, IMPLEMENTATIONS BROKEN** — all 4 routes in /api/luxeforge/measurements/* fail | NO — LuxeForgePage doesn't call them either |
| avatar | /api/v1/avatar/chat | 405 | 31 | POST only | NO |
| emails | /api/v1/emails/preview/welcome | 405 | 31 | POST only | NO |
| recovery-core | /api/v1/recovery-core/connectivity | 200 | 306 | **YES** (cloudflare tunnel status) | NO |
| patterns | /api/v1/patterns/box-cushion | 405 | 31 | POST only — drawing-renderer templates | NO |
| finance-legacy | /api/v1/finance-legacy/dashboard | 200 | 262 | **YES** (totals + pipeline) | NO — `finance` (24 routes) is used by workroom page; `finance-legacy` is the OLD path |

### Orphan categories

**A. Real+working but invisible to MAX (the dispatch's "dangerous ones"):**
- **fabrics** (8 routes, 12 rows of real data) — workroom-vocabulary inventory of drapery fabrics with suppliers, costs, swatches. Currently unreachable from any nav entry. The dispatch specifically flagged workroom vocabulary as a target; this fits exactly.
- **work-orders** (5 routes, 0 rows) — endpoint alive but no data; if business ever starts work orders, they're invisible
- **finance-legacy** (8 routes, real dashboard data) — old finance router still alive alongside the new `/finance`; counts as a phantom duplicate
- **maintenance** (11 routes) — has a maintenance scheduler with 17 pending tasks per /status; "maintenance_log" table has 18 rows. Unreachable from UI.
- **recovery-core** (9 routes, real connectivity data) — companion to RecoveryForge
- **notifications** (7 routes, 1 active notification) — already used inline by RightPanel; no nav entry of its own
- **orchestration** (under /max/orchestration, 30KB of role/entrypoints config) — system wiring map, only readable as JSON
- **patterns** (14 routes, drawing templates) — POST-only drawing primitives; not wired to DrawingStudio UI

**B. Endpoint-registered-but-broken (silent failures):**
- **measurements** (`/api/luxeforge/measurements/*`, 4 routes) — all 4 routes return 404 on GET and 422 on POST. **The OpenAPI advertises capability the backend does not implement.** This is the dispatch's "production capability unreachable from the UI" case.

**C. POST-only stubs (no GET-able state):**
- **ai** (`/ai/ai/*`, 18 routes) — text-rewrite endpoints, all POST
- **avatar** (4 routes) — POST-only avatar chat
- **emails** (4 routes) — POST-only template renderer

---

## CROSS-CUTTING FINDINGS (not asked but unavoidable)

1. **DB/API mismatches:** `/api/v1/tickets/` returns empty envelope (`{"tickets":[],"total":0}`) but the `sf_tickets` table has 3 rows. Either the API queries a different table, or there is a tenancy filter. Worth a follow-up probe with auth.

2. **LeadForge table vs router mismatch:** `prospects` table has 322 rows but `lf_prospects` and `lf_leads` are both 0. Either prospects were imported before the `lf_` prefix existed, or the router is querying the wrong table. Either way the LeadForge nav entry is wired to empty data while real prospect data sits one table away.

3. **vision's component is mis-wired:** the `VisionAnalysisPage` (claim: active) only fetches `/crm/customers`. The `/api/v1/vision/*` backend (10 routes, /status works) is unreachable from the page itself. This is a real defect, not a status-fudging issue.

4. **AMP page is a static SPA:** `amp/page.tsx` is 489 lines of hardcoded HUBSPOT_BASE imagery and copy. Zero fetch calls. The /api/v1/amp backend (15 routes) is therefore also unreachable from this page; the AMP backend may serve a separate deployable app at port 3003.

5. **business-profile routes via /socialforge not /businessops:** `BusinessProfileScreen` calls `${API}/socialforge/profile`. The `businessops` backend prefix (16 routes) is unrelated. The component's URL is a name-divergence — not a bug, just naming drift.

6. **OpenClaw "active" but practically broken:** dispatch asserts ~80% failure rate at 27 tasks / 57 days; the API still returns 7390 openclaw_tasks rows (the dispatch brief prints `Tasks: 0 open` for 8 days because of the failure rate, not because tasks are absent). The "active" claim is true at the data layer (data flows) but false at the working layer.

7. **Luxe + Market auth-bound:** both use 401 endpoints. Auth flow may be token-based (founder cookie). A quick follow-up with a token would resolve them to LIVE or WIRED-EMPTY.

---

## VERIFIED
- All 37 nav entries extracted from `LeftNav.tsx:40-128`; switch mapped in `app/page.tsx:429-489`.
- Static grep of every component's `fetch(` and `/api/v1` references (full call inventory in §2.2).
- Live GET of one representative route per nav entry (HTTP code, body size, and emptiness inspection in §2.3).
- DB row counts for all 147 tables (§1.3 listing + §2.3 targeted subset).
- 16 orphan prefixes probed; OpenAPI walked for correct paths per prefix.
- /api/luxeforge/measurements/* verified broken via GET=404 + POST=422 on all 4 routes.
- vetforge/petforge confirmed as 83-line static placeholders (read full content, no fetches).
- ContinuityPanel call inventory confirms max-continuity is NOT a shell.
- AMP page (489 lines) confirmed to contain zero fetch calls.
- Backend PID 967507 up (~21 min uptime at write time), portal :3005 HTTP 200.

## INFERRED
- `assist` (claimed dev) is functionally LIVE; the founder under-claimed it.
- `hardware` (claimed dev) is functionally LIVE; it inherits the generic PRODUCT_MAP page which probes `/system/stats` successfully.
- `dev` (Developer Panel, claimed dev) is LIVE; `/dev/status` returns 365 bytes of real config.
- LeadForge's "active" claim is supported by the component wiring but contradicted by the router's empty response; the 322 prospects row count is in a table the router does not query.

## COULD NOT PROBE
- **luxe** and **market** nav entries: both hit 401 on their first GET. Without an auth token I cannot confirm WIRED-EMPTY vs LIVE. DB strongly suggests both would be LIVE behind auth (`intake_projects`=503, `listings`=2, `mf_products`=11).
- `drawings` Live-or-not: dispatch notes Roman shades live; my probes only hit POST-only endpoints with 405 on GET, which is consistent with LIVE but not positive confirmation.
- `transcript` chunk review endpoints: not probed in detail (the route listing confirms 11 routes live).

---

## HEADLINE TOTALS

```
NAV ENTRIES: 37
CLAIMED ACTIVE: 32
CLAIMED DEV:    3   (hardware, assist, dev)
CLAIMED PLANNED: 2  (vetforge, petforge)

OBSERVED LIVE:           26
OBSERVED WIRED-EMPTY:     5   (lead, pay, support, ship, llc)
OBSERVED SHELL:           4   (vision, amp, vetforge, petforge)
OBSERVED UNKNOWN (auth):  2   (luxe, market)
OBSERVED ABSENT:          0

CLAIMED != OBSERVED (strict):  9   (all of WIRED-EMPTY, SHELL-from-active, UNKNOWN-from-active)
CLAIMED != OBSERVED (with UNKNOWN): 11

REAL+WORKING BUT IN NO REGISTRY/NAV:  8
  fabrics (12 rows, workroom vocabulary, dispatch-flagged)
  work-orders (0 rows, capability latent)
  finance-legacy (262b dashboard, phantom duplicate)
  maintenance (11 routes, 17 pending tasks)
  recovery-core (9 routes, connectivity data)
  notifications (7 routes, 1 active notification)
  orchestration (under /max, 30KB config)
  patterns (14 routes, drawing templates)

ENDPOINT-REGISTERED BUT BROKEN (silent failure):  1
  measurements (/api/luxeforge/measurements/*, 4 routes, all 404/422/405)
```

🛑 **STOP.** Part 2 complete. Awaiting founder go-ahead before Part 3 (the five-way reconciliation).

---

## PART 3 — THE FIVE-WAY RECONCILIATION

### 3.1 — Sources located (and confirmed still in conflict)

Per dispatch §3.1, locating each inventory:

| # | source | location | line/file | count |
|---|---|---|---|---|
| 1 | **authoritative registry (7)** | `backend/app/services/max/operating_registry.json` | top-level `ecosystem_products[]` array | **7** (max, relistapp, finance, openclaw, email, workroom_creations, supportforge) |
| 2 | **catalog (22)** | `backend/app/services/max/ecosystem_catalog.py` | `EMPIRE_CATALOG["products"]` dict | **22** |
| 3 | **modules in code (~24)** | `EMPIRE-MODULE-READINESS-REGISTRY.md` (root) — generated 2026-06-09, NOT 2026-06-26 | Sections 1-3 | **37 in main 3 sections** (43 including Section 4 execution surfaces). The "~24" is not reproduced here — possibly an earlier count. **The dispatch's "~24" disagrees with the readiness registry I found.** |
| 4 | **left nav (37)** | `empire-command-center/app/components/layout/LeftNav.tsx` | lines 40-128 | **37** |
| 5 | **navigator graph nodes (63)** | **NOT FOUND** — searched `backend/app/services/max/`, `backend/app/routers/`, `empire-command-center/app/`, `ecosystem_catalog.py`, `system_prompt.py`. The number 63 does not appear anywhere in the codebase. Closest current figures: `total_products:22 + total_tools:38 = 60`, or `22 products + 7 surfaces + 13 integrations + 9 services + 5 desks ≈ 56`. None sum to 63. **UNKNOWN source.** | — | **UNKNOWN** |

The three "drift" registries named in `CLAUDE_label_station.md` all still exist and still disagree:

- **`_EMPIRE_MODULES` in `routers/max/router.py:1648`** — Python list of 7 strings: `["MAX", "Empire Workroom", "Woodcraft", "Model Selector / AI Distributor Hub", "Tokens & Costs", "OpenClaw", "Hermes"]`. Hardcoded literal, used in link-intelligence heuristics.
- **`empire_module_knowledge.py` `MODULE_ALIASES` (lines 24-43)** — Python dict of 10 module names with alias tuples: ArchiveForge, MarketForge, Workroom, Drawing Studio, RecoveryForge, RelistApp, VendorOps, SocialForge, Hermes, OpenClaw.
- **`operating_registry.json` ecosystem_products** (file mtime: `2026-05-15 09:23:27` — ~3 months unchanged; `updated_at` field inside the JSON: `2026-04-19`, so the JSON content was last edited by hand ~4 months ago, and the file was last touched on disk 3 months ago — they STILL disagree).

**Disagreement confirmation:** the three drift registries agree on **only 2 modules**: OpenClaw (in all 3) and Hermes (in 2 of 3). Everything else is a name-divergence: `_EMPIRE_MODULES` says "Empire Workroom" while `MODULE_ALIASES` says "Workroom" while `EMPIRE_CATALOG` says "WorkroomForge" — same entity, three spellings.

### 3.2 — The reconciliation table

Union of every module name across all 5 confirmed sources (the 5th "navigator" is UNKNOWN, see §3.1):

| MODULE | nav (37)? | registry (7)? | catalog (22)? | router `_EMPIRE_MODULES` (7)? | has backend routes? | has data? | OBSERVED |
|---|---|---|---|---|---|---|---|
| MAX | owner (proxy) | ✓ max | ✓ command_center | ✓ MAX | yes (`/api/v1/max/*` 79 routes) | yes (token_tracker, memories, chat) | LIVE |
| Empire Workroom | ✓ workroom | — | ✓ workroom_forge | ✓ "Empire Workroom" | yes (`/quotes-v2`, `/crm`, `/jobs`, `/finance`) | yes (49 quotes_v2, 171 customers, 8 jobs) | LIVE |
| WoodCraft / CraftForge | ✓ craft | — | ✓ craft_forge | ✓ "Woodcraft" | yes (`/api/v1/craftforge/*` 14 routes) | yes (3 projects, 60 lots) | LIVE |
| OpenClaw | ✓ openclaw | ✓ openclaw | — | ✓ OpenClaw | yes (13 routes) | yes (7390 tasks) | LIVE (data layer) — broken at execution layer per dispatch |
| Hermes | — | — | — | ✓ Hermes | (in services/max/) | yes | LIVE (in MAX bundle; not a nav product) |
| Tokens & Costs | ✓ tokens | — | — | ✓ "Tokens & Costs" | yes (`/costs/*` 15 routes) | yes | LIVE |
| Model Selector / AI Distributor Hub | — | — | — | ✓ (entry) | (built into MAX system_prompt) | yes | LIVE |
| Owner / Owner's Desk | ✓ owner | — | — | — | yes (uses DashboardScreen / /max/accuracy/stats) | yes (944b stats) | LIVE |
| ForgeCRM | ✓ crm | — | ✓ forge_crm | — | yes (`/crm/*` 6 routes) | yes (171 customers) | LIVE |
| VendorOps | ✓ vendorops | — | — | — | yes (25 routes) | yes (51 vendors, vo_subscriptions=1) | LIVE |
| LeadForge | ✓ lead | — | ✓ lead_forge | — | yes (`/leads/*` 55 routes) | empty (`/api/v1/leads/` returns 0; `prospects`=322 in AMBIGUOUS table) | **WIRED-EMPTY** |
| MarketForge | ✓ market | — | ✓ market_forge | — | yes (some routes; `/listings` 401) | yes (mf_products=11, listings=2 behind auth) | UNKNOWN (auth) |
| SocialForge | ✓ social | — | ✓ social_forge | — | yes (21 routes) | yes (12 social_accounts) | LIVE |
| SupportForge | ✓ support | ✓ supportforge | ✓ support_forge | — | yes (5 routes) | empty (`/api/v1/tickets/` returns 0; `sf_tickets`=3 in DB) | **WIRED-EMPTY** |
| ShipForge | ✓ ship | — | ✓ ship_forge | — | yes (`/shipping/*` 6 routes — no `/api/v1/`) | empty (shipments=0; `shipping.py` is self-declared PLACEHOLDER) | **WIRED-EMPTY** |
| AMP | ✓ amp | — | ✓ amp | — | yes (`/api/v1/amp/*` 15 routes) | empty (`data/amp.db` empty; `amp/page.tsx` is 489-line static SPA) | **SHELL** (component-side) |
| ArchiveForge | ✓ archive | — | ✓ recovery_forge (separate from archforge) | — | yes (77 routes) | yes (73 archives, 1863 life_issues) | LIVE |
| RecoveryForge | ✓ recovery | — | ✓ recovery_forge | — | yes (24 routes) | yes (Layer 3 running) | LIVE |
| TranscriptForge | ✓ transcript | — | ✓ (under tools) | — | yes (29 routes) | yes | LIVE |
| Drawing Studio | ✓ drawings | — | ✓ drawing studio | — | yes (22 POST routes) | yes (Roman shades live per dispatch) | LIVE |
| Pricing Studio | ✓ pricing-studio | — | ✓ pricing studio | — | yes (`/api/v1/pricing/*` 7 routes) | yes | LIVE |
| AI Vision | ✓ vision | — | ✓ vision_analysis | — | yes (10 routes) | yes (status works) | **SHELL** (component fetches wrong route) |
| RelistApp | ✓ relist | ✓ relistapp | ✓ relist_app | — | yes (37 routes) | yes (18 ra_listings, 16 services) | LIVE |
| EmpireAssist | ✓ assist | — | ✓ empire_assist | — | frontend only; no dedicated router | uses `/costs/transactions` | LIVE (reads costs table) |
| EmpirePay | ✓ pay | — | ✓ empire_pay | — | yes (`/crypto-payments/*` 9 routes) | empty (crypto_transactions=0, crypto_wallets=0); `/finance/invoices` part has 32 rows | **WIRED-EMPTY** (crypto portion only) |
| LLCFactory | ✓ llc | — | ✓ llc_factory | — | yes (`/llcfactory/*` 15 routes) | partial (services/packages data; llc_formations=0) | **WIRED-EMPTY** |
| ApostApp | ✓ apost | — | ✓ apost_app | — | yes (17 routes) | partial (apostille_documents=0; orders endpoint has data) | LIVE |
| ConstructionForge | ✓ construction | — | ✓ (under tools) | — | yes (32 routes) | yes (3 cf_projects) | LIVE |
| StoreFront Forge | ✓ storefront | — | — | — | yes (32 routes) | yes (50 sf2_products) | LIVE |
| ContractorForge | ✓ contractor | — | ✓ contractor_forge | — | uses shared routes (`/jobs`, `/contacts`, `/finance/invoices`) | yes (8 jobs, 7 contacts, 32 invoices) | LIVE |
| LuxeForge | ✓ luxe | — | ✓ luxe_forge | — | yes (intake admin, luxeforge_measurements) | yes (503 intake_projects behind auth) | UNKNOWN (auth) |
| Business Profile | ✓ business-profile | — | — | — | uses `/socialforge/profile` | yes (business_profiles=2) | LIVE |
| PlatformForge | ✓ platform | — | — | — | yes (`/system/*` 6 routes + /businessops 16) | yes | LIVE |
| System | ✓ system | — | — | — | yes (`/max/system-report`) | yes (5544b payload) | LIVE |
| Hardware | ✓ hardware | — | — | — | (no hardware-specific; uses `/system/stats`) | yes (1508b payload) | LIVE (via generic PRODUCT_MAP) |
| MAX Continuity | ✓ max-continuity | — | — | — | uses `/max/status`, `/max/evaluation/scores`, etc. | yes (ContinuityPanel fetches live) | LIVE |
| Daily Summary | ✓ daily-summary | — | — | — | RightPanel fetches 8+ `/max/*` endpoints | yes | LIVE |
| Developer Panel | ✓ dev | — | — | — | yes (`/dev/*` 6 routes) | yes | LIVE |
| VetForge | ✓ vetforge | — | ✓ vet_forge | — | NONE | none | **SHELL** (placeholder) |
| PetForge | ✓ petforge | — | ✓ pet_forge | — | NONE | none | **SHELL** (placeholder) |
| Smart Lister | — | — | ✓ smart_lister | — | (no nav entry; backend may exist) | unknown | UNKNOWN |
| Workroom Creations Lab | — | ✓ workroom_creations | — | — | NONE | n/a | quarantined (operating_registry truth) |
| **fabrics** (orphan router) | — | — | — (catalog has 4 generic mentions) | — | yes (`/api/v1/fabrics` 8 routes) | yes (12 fabric rows) | **LIVE but invisible to all 5 inventories** |
| **work-orders** (orphan router) | — | — | — (catalog has 1 mention) | — | yes (`/api/v1/work-orders` 5 routes) | empty (0 rows) | **WIRED-EMPTY, invisible to all 5 inventories** |
| **measurements** (orphan broken router) | — | — | — | — | yes (registered, `/api/luxeforge/measurements/*` 4 routes) | BROKEN (all 404/422/405) | **SHELL but invisible to all 5 inventories** |
| **maintenance** (orphan router) | — | — | — | — | yes (11 routes) | yes (17 pending maintenance tasks; `maintenance_log`=18) | **LIVE but invisible** |
| **notifications** (orphan router) | — | — | — | — | yes (7 routes) | yes (1 active notification) | **LIVE but invisible** |
| **orchestration** (orphan under /max) | — | — | — | — | yes (`/api/v1/max/orchestration/*` 9 routes) | yes (30KB config: role, entrypoints) | **LIVE but invisible** |
| **recovery-core** (orphan router) | — | — | — | — | yes (9 routes) | yes (cloudflare tunnel connectivity) | **LIVE but invisible** |
| **finance-legacy** (orphan duplicate router) | — | — | — | — | yes (8 routes) | yes (262b dashboard) | **LIVE but invisible** |
| **patterns** (orphan POST-only router) | — | — | — | — | yes (14 routes, POST only) | n/a | **drawings-adjacent, invisible** |
| **avatar** (orphan POST-only router) | — | — | — | — | yes (4 routes, POST only) | n/a | **presentation-adjacent, invisible** |
| **emails** (orphan POST-only router) | — | — | — | — | yes (4 routes, POST only) | n/a | **email-adjacent, invisible** |
| **qr** (orphan router) | — | — | — | — | yes (9 routes) | yes (cookie-policy returns 708b) | **LIVE but invisible** |
| **portal** (orphan under payments) | — | — | — | — | yes (`/api/v1/payments/portal`) | unknown | **payments-adjacent, invisible** |
| **lifecycle** (nested in archiveforge) | — | — | — | — | nested routes under `/api/v1/archiveforge/{id}/lifecycle` | yes (`archive_item_lifecycle`=4) | **accessible via archiveforge page** |

### 3.2.1 — Four gap lists

**A. Modules in ALL inventories (the agreed core):**
- Only **OpenClaw** appears in all 5 confirmed sources (nav, operating_registry, EMPIRE_CATALOG, `_EMPIRE_MODULES`, and implicitly via MODULE_ALIASES).
- **MAX** appears in 4 of 5 (operating_registry, `_EMPIRE_MODULES`, EMPIRE_CATALOG as `command_center`, and the nav indirectly via "owner"/chat). Module registry explicitly notes "MAX is Founder AI brain."
- **RecoveryForge** appears in nav, EMPIRE_CATALOG, and MODULE_ALIASES — not in operating_registry or `_EMPIRE_MODULES`.

**B. Modules in nav only (nav lists but no other inventory names them):**
- `business-profile` (only nav + OperatingRegistry's email/socialforge/profile route)
- `hardware` (only nav; readiness registry notes "Hardware — experimental — docs only")
- `system` (only nav; the docs registry has it under PlatformForge/SystemMonitor)
- `tokens` (nav has `tokens`; operating_registry has `finance` not `tokens`; `_EMPIRE_MODULES` lists "Tokens & Costs" as a string)
- `pricing-studio` (only nav; the docs registry has it under "Drawing Studio / Pricing Engine" merged)
- `max-continuity` (only nav; the docs registry has it as "MAX Continuity — later-addon")
- `daily-summary` (panel, not a module per any inventory)
- `dev` (Developer Panel — only nav; docs registry has it under "System")
- `vetforge` and `petforge` (nav + EMPIRE_CATALOG only; no backend, no routes)

**C. Real+working but in NO registry (the dispatch's "dangerous ones"):**
- **fabrics** (8 routes, 12 fabric rows — workroom vocabulary)
- **work-orders** (5 routes, 0 rows — capability latent)
- **measurements** (4 routes registered but BROKEN — silent failure)
- **maintenance** (11 routes, 17 pending tasks)
- **recovery-core** (9 routes, connectivity data)
- **notifications** (7 routes, 1 active notification)
- **orchestration** (under /max/orchestration, 30KB config)
- **patterns** (14 POST routes, drawing primitives)
- **finance-legacy** (8 routes, dashboard data, phantom duplicate of `/finance`)
- **qr** (9 routes, cookie-policy data)
- **portal** (under /api/v1/payments/portal)
- **lifecycle** (nested in archiveforge, 4 lifecycle rows in DB)
- **avatar** (4 POST routes)
- **emails** (4 POST routes)

**14 orphan capabilities with no representation in any of the 5 inventories.**

**D. Modules in a registry that exist nowhere else:**
- **workroom_creations** (operating_registry only) — quarantined, `broken_quarantined`, no routes — accurate
- **Model Selector / AI Distributor Hub** (`_EMPIRE_MODULES` only) — exists in code under `/max/system_prompt.py` AI routing; not a standalone product anywhere else
- **smart_lister** (EMPIRE_CATALOG only) — no nav entry, no clear backend prefix; possibly RelistApp-adjacent
- **Hermes** (`_EMPIRE_MODULES` + MODULE_ALIASES) — runtime surface, no standalone backend prefix; lives in `backend/app/services/max/hermes_*`

### 3.3 — What MAX itself believes (per dispatch §3.3)

The registry's mtime was claimed to be **2026-05-15** — confirmed:

```
$ stat -c '%n %y' backend/app/services/max/operating_registry.json
backend/app/services/max/operating_registry.json 2026-05-15 09:23:27.845302158 -0400
```

But the file's own `updated_at` field says `2026-04-19`, so the JSON content was last hand-edited on 2026-04-19 and the file was last touched on disk on 2026-05-15 (~3 months before this dispatch, 2026-08-22).

**What MAX's own registry believes about EmpireBox:**

| ecosystem_product | status | routes | max_can_query | max_can_act | openclaw_delegate | truthful_ctas |
|---|---|---|---|---|---|---|
| max | active_partial | /max/chat, /max/chat/stream, /chats/memory-bank | ✓ | ✓ | ✓ | ✓ |
| relistapp | active_partial | /relist/* (6 listed) | ✓ | ✓ | ✓ | ✓ |
| finance | active_partial | /finance/dashboard, /finance/invoices, /finance/payments, /payments/checkout, /payments/webhook | ✓ | ✓ | ✓ | ✓ |
| openclaw | active_partial | /openclaw/tasks, /openclaw/tasks/stats, /openclaw/health | ✓ | ✓ | ✓ | ✓ |
| email | partial | /max/gmail/inbox, /emails/send, /webhooks/email/inbound | ✓ | ✓ | ✗ | **false** |
| workroom_creations | broken_quarantined | (none) | ✗ | ✗ | ✗ | ✓ |
| supportforge | partial | /tickets, /customers, /kb | ✓ | ✓ | ✓ | **false** |

**Modules MAX knows about: 7** (per operating_registry.json `ecosystem_products`).

**Modules in the LeftNav: 37** (per Part 1.1).

**Modules MAX knows about vs. modules in the nav:** **30 nav entries are invisible to MAX.** Some of those 30 are real+working modules the nav exposes but MAX's registry does not list, including: Empire Workroom (split as workroom + finance in registry), WoodCraft (in nav, not registry), LuxeForge (nav + catalog only), LeadForge, MarketForge, SocialForge, ShipForge, AMP, ArchiveForge, RecoveryForge, TranscriptForge, Drawing Studio, AI Vision, EmpireAssist, EmpirePay, LLCFactory, ApostApp, ConstructionForge, StoreFront Forge, ContractorForge, Pricing Studio, Business Profile, PlatformForge, System, MAX Continuity, Tokens & Costs, Hardware, Daily Summary, Developer Panel, VetForge, PetForge.

### 3.4 — The autonomy-blocker confirmation (per founder's added question)

> **"Confirm whether operating_registry.json knows about fabrics, work-orders, or measurements."**

```
$ grep -nE "fabric|work-order|measurement" backend/app/services/max/operating_registry.json
(no matches)
```

**Confirmed: operating_registry.json mentions ZERO of fabrics, work-orders, or measurements.**

This is in one line: **MAX's own registry omits 14 real+working capabilities** (the full list in §3.2.1.C), including the workroom-vocabulary fabrics router that the dispatch explicitly flagged. MAX cannot autonomously operate a system whose own registry does not list 14 working routers, because every MAX capability-claim check consults this registry first.

**Concrete consequence:**
- Founder asks MAX "can you list my fabric inventory?" → MAX checks operating_registry, finds no `fabrics` product, returns UNKNOWN or refuses.
- Founder asks MAX "what work orders are open?" → MAX checks operating_registry, finds no `work-orders` product, returns UNKNOWN or refuses.
- Founder asks MAX "show measurements for this drapery job?" → MAX checks operating_registry, finds no `measurements` product, returns UNKNOWN — but `/api/luxeforge/measurements/*` IS registered (4 routes), even though they are broken on the backend.

---

## EFFORT CLASSIFICATION — the 9 claimed≠observed entries (added per founder instruction)

> Founder's classification: **(a)** wrong table/route in one file, **(b)** missing component wiring, **(c)** genuinely unbuilt. **Effort only, no retirement recommendation.**

| nav id | claimed → observed | effort class | specific fix locus | one-line diagnosis |
|---|---|---|---|---|
| **vision** | active → SHELL | **(b) missing component wiring** | `VisionAnalysisPage.tsx` (one component file) | Replace `/crm/customers` calls with `/api/v1/vision/*` calls; the backend exists (10 routes). One-component edit. |
| **amp** | active → SHELL | **(b) missing component wiring** *(with caveat)* | `app/amp/page.tsx` (489-line static SPA) | Page has zero fetch calls; readiness registry notes AMP has separate `data/amp.db` and is "Safe to ship as customer-facing standalone" — meaning the real AMP app may be a separate deployable at port 3003. If CC's `amp/page.tsx` is meant to be the AMP dashboard, wire `/api/v1/amp/*` (15 routes); if it's just a redirect, change nav claim from "active" to "lobby" or remove the entry. One file either way. |
| **support** | active → WIRED-EMPTY | **(a) wrong table/route in one file** | `supportforge_tickets.py` router (one file) | `/api/v1/tickets/` returns empty envelope `{"tickets":[],"total":0}` despite `sf_tickets`=3. The router is querying a different table or has a tenancy filter. Either change table name or remove the filter — single-file fix. |
| **lead** | active → WIRED-EMPTY | **(a) wrong table/route in one file** | `leadforge.py` router (one file) | `/api/v1/leads/` queries `lf_leads` (0 rows) but `prospects` table has 322 rows. Change the FROM clause to `prospects` (or add a JOIN). Single-file fix. |
| **pay** | active → WIRED-EMPTY | **(c) genuinely unbuilt** | (no one-file fix exists) | Crypto payments need a chain provider (Stripe crypto? Coinbase Commerce?), wallet config, and a customer-facing flow. Zero data in `crypto_transactions`/`crypto_wallets`. The `/finance/invoices` portion of the page IS live (32 rows) — that part is working, the crypto side is not. Building crypto payments is a project, not a fix. |
| **ship** | active → WIRED-EMPTY | **(c) genuinely unbuilt** | (no one-file fix exists) | `shipping.py` is **self-declared PLACEHOLDER** per readiness registry line 30: "EasyPost stub, hardcoded test rates, fake tracking." Needs EasyPost API integration. A project. |
| **llc** | active → WIRED-EMPTY | **(c) genuinely unbuilt** | (no one-file fix exists) | `llc_formations`=0 because the partner integration with Northwest Registered Agents is not wired (readiness registry line 11). The services/packages catalog side works; the actual formation flow needs the partner connection. A project. |
| **luxe** | active → UNKNOWN | **AMBIGUOUS — likely (a) wrong auth wiring** | `intake_auth.py` or the page's fetch wrapper | 401 on `/api/v1/intake/admin/projects`. Cannot confirm without auth token. The DB strongly suggests LIVE behind auth (`intake_projects`=503, `intake_users`=654). If auth wiring is broken in one file (e.g. token not sent by `LuxeForgePage.tsx`), it's (a); if the page needs founder role to view, it's correctly behind auth. **Cannot classify without probing with auth.** |
| **market** | active → UNKNOWN | **AMBIGUOUS — likely (a) wrong auth wiring** | `MarketForgePage.tsx` or `marketforge_products.py` | 401 on `/listings` (note: NOT `/api/v1/listings` — at root). Same auth ambiguity as luxe. DB has 2 listings + 11 mf_products. **Cannot classify without probing with auth.** |

### Effort-class tally (the "afternoon vs project" question)

| effort class | count | entries | what it means |
|---|---|---|---|
| **(a) wrong table/route in one file** | 2 | support, lead | **Two afternoon fixes.** Both are single-router edits: change table name or remove filter. |
| **(b) missing component wiring** | 2 | vision, amp | **Two afternoon-to-day fixes.** Both are single-component edits, but require understanding what the page is meant to do (especially amp). |
| **(c) genuinely unbuilt** | 3 | pay, ship, llc | **Three projects.** Each requires a partner integration or a new flow build-out. Not fixable in a file edit. |
| **AMBIGUOUS** | 2 | luxe, market | **One session to resolve.** A founder-token auth probe would likely flip both to LIVE behind auth. If auth is the actual wiring problem, both become (a). |
| **TOTAL** | **9** | | |

**Net:**
- **4 of 9** are afternoon-scale fixes (2 file edits + 2 wiring edits)
- **2 of 9** resolve with one auth probe
- **3 of 9** are genuine projects

The dispatch's "afternoon vs project" framing maps cleanly:
- Afternoon pile: vision, amp, support, lead (plus luxe + market once auth is confirmed).
- Project pile: pay, ship, llc.

---

## VERIFIED
- All 5 inventory sources located except navigator graph nodes (UNKNOWN source).
- operating_registry.json confirmed as the "authoritative registry (7)": 7 ecosystem_products (max, relistapp, finance, openclaw, email, workroom_creations, supportforge).
- EMPIRE_CATALOG confirmed as "catalog (22)": 22 products.
- `_EMPIRE_MODULES` (router.py:1648) confirmed: 7 strings.
- `MODULE_ALIASES` (empire_module_knowledge.py:24-43) confirmed: 10 modules.
- The three "drift" registries still exist and still disagree; only OpenClaw appears in all 3 drift registries; only 2 modules (OpenClaw + Hermes) appear in 2 of 3.
- operating_registry.json mtime confirmed `2026-05-15 09:23:27` (file); JSON `updated_at` field `2026-04-19` — both old enough to be stale.
- operating_registry.json does NOT mention fabrics, work-orders, or measurements (grep confirmed zero hits).
- 14 orphan capabilities identified with no representation in any of the 5 inventories.
- 9 strict claimed≠observed entries classified by effort class.

## INFERRED
- The "63 navigator graph nodes" figure in the dispatch brief has no findable source in the current codebase. Either it referred to a defunct MAX tool, an older runtime state, or a hypothetical graph build that hasn't been instantiated. Reported as UNKNOWN.
- The "~24 modules in code" count from the dispatch brief does not match the readiness registry's 37-43 modules; the dispatch may have referenced an earlier assessment that no longer exists.
- MAX cannot autonomously operate a system whose own registry omits 14 working routers (the autonomy blocker in one line, per founder).
- Of the 9 mismatches, 4 are afternoon-scale fixes and 2 resolve with one auth probe; only 3 are projects.

## COULD NOT PROBE
- **luxe** and **market** could not be classified WIRED-EMPTY vs LIVE because both endpoints return 401. A founder-token auth probe would resolve them. The DB strongly suggests LIVE behind auth.
- The exact provenance of "63 navigator graph nodes" and the dispatch's "~24 modules in code" count.
- The relationship between `recovery-core` and `recovery` — both prefixes exist with distinct routers; the dispatch did not probe whether they are aliases or duplicates.
- Whether `finance-legacy` is intentionally kept warm as a rollback target or is dead code that should be retired.

---

## HEADLINE TOTALS (Part 3)

```
NAV ENTRIES:                                                  37
operating_registry.json ecosystem_products (authoritative):   7
EMPIRE_CATALOG products (catalog):                           22
_EMPIRE_MODULES (router.py:1648):                             7
MODULE_ALIASES (empire_module_knowledge.py):                 10
navigator graph nodes (claimed):                             UNKNOWN — no source in current repo

MODULES IN ALL INVENTORIES (the agreed core):                 1   (OpenClaw only)
MODULES IN NAV ONLY (no other inventory names them):         12
MODULES REAL+WORKING BUT IN NO REGISTRY (the autonomy pile): 14
MODULES IN A REGISTRY THAT EXIST NOWHERE ELSE:                4   (workroom_creations, Model Selector, smart_lister, Hermes)

OPERATING_REGISTRY mentions fabrics:                          NO
OPERATING_REGISTRY mentions work-orders:                      NO
OPERATING_REGISTRY mentions measurements:                     NO

EFFORT CLASSIFICATION (9 claimed≠observed entries):
  (a) wrong table/route in one file:                           2  (support, lead)
  (b) missing component wiring:                                2  (vision, amp)
  (c) genuinely unbuilt:                                       3  (pay, ship, llc)
  AMBIGUOUS (likely (a), needs auth probe):                    2  (luxe, market)
```

🛑 **STOP.** Part 3 complete. The autonomy blocker in one line: **MAX's own operating_registry.json omits 14 real+working capabilities, including fabrics (12 rows, workroom vocabulary), work-orders (5 routes), and measurements (4 routes, broken).** Awaiting founder ruling.


